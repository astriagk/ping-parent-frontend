logo.png is a placeholder (base64 text). Replace with a real PNG at this path:

assets/logo.png

If you add a binary PNG here, update `src/components/Logo.tsx` to use:

<Image source={require('../assets/logo.png')} ... />

(or adjust the relative path as needed).
